package com.ei.pwwdseljava.onlineinvoice.tests;

//import com.worksafe.onlineinvoice.utils.ExtentReportUtils;
import org.testng.annotations.Test;

public class InvoiceEntryTest {

    @Test
    public void testInvoiceEntry() {
       // ExtentReportUtils.startTest("Invoice Entry Test", "Verifying invoice entry page functionality");

        //try {
            // Your test actions go here (e.g., filling out the invoice form)

            // Log the success of the test
           // ExtentReportUtils.logPass("Invoice entry page opened successfully.");

      //  } catch (Exception e) {
            // Log the failure if something goes wrong
           // ExtentReportUtils.logFail("Test failed: " + e.getMessage());
        }

        // Flush the report and send email after the test
		/*
		 * ExtentReportUtils.flush(); ExtentReportUtils.sendEmailReport();
		 */
   // }
}
